# 원본 소재 — 대외비 PPT(224MB)와 미압축 영상. 저장소에 올리지 않는다.
_원본/
*.pptx
*.mov

# 작업 백업 — 편집 모드가 저장 직전 원본을 .bak 으로 남긴다
*.bak
*.imgbak
*~

# OS
Thumbs.db
desktop.ini
.DS_Store

# 작업용 문서·도구 상태 — 저장소는 public 이라 올리지 않는다
# PRODUCT.md 는 채용 공고 분석과 포지셔닝 전략이 들어 있어 담당자에게 보이면 안 된다
PRODUCT.md
작업메모.md
.impeccable/
.claude/skills/
.claude/settings.local.json
