# 디자인 시스템 — 코리아펠라직 지원 포트폴리오

이 문서는 **일관성 계약서**입니다. 마크업을 쓰기 전에 여기서 값을 찾고, 여기 없는 값은
하드코딩하지 않고 이 문서에 먼저 추가합니다.

- **원본** — `index.html` 하나. 웹사이트가 원본이고 PDF는 인쇄 스타일시트로 파생됩니다.
- **참고 대상** — 브랜드 무드는 [가시제거연구소](https://mackerel.co.kr) 실측.
  기존 포트폴리오(`claude share/portfolio`)에서는 **내용과 소재만** 가져오고
  레이아웃·디자인은 따르지 않습니다.

---

## 1. 브랜드 근거

mackerel.co.kr 을 브라우저로 열어 computed style 을 실측한 결과입니다.

| 항목 | 실측값 |
|---|---|
| 액센트 | `rgb(255,143,28)` = **`#FF8F1C`** — 배경으로만 60회 이상. 사실상 유일한 브랜드 컬러 |
| 텍스트 | `#000` · `rgb(53,53,53)` · `rgb(153,153,153)` |
| 면 | `#FFF` · `rgb(245,245,245)` · `rgb(222,222,222)` |
| 폰트 | **Pretendard Variable** |
| 라운드 | `20px` (72회) · **`20px 0` 비대칭 (47회)** · `50px` 알약 (12회) · `50%` 원형 |

**해석** — 이 브랜드의 "귀여움"은 일러스트나 파스텔이 아니라
**오렌지 + 큰 라운드 + 비대칭 코너** 세 가지입니다. 그래서 우리도 이 셋만 가져옵니다.

**무드 강도는 "중간 — 따뜻한 전문성"으로 확정.**
오렌지는 액센트·태그·밴드·진행바에만 쓰고 본문은 검정 계열을 유지합니다.
포트폴리오는 신뢰가 먼저입니다. 브랜드 결은 느껴지되 주인공은 수치와 근거입니다.

---

## 2. 색

### 2-1. 토큰

```css
:root{
  /* 브랜드 */
  --brand:        #FF8F1C;  /* 큰 면·태그·밴드·진행바 */
  --brand-ink:    #B85A05;  /* 흰 배경 위 오렌지 '텍스트' */
  --brand-mark:   #D2700A;  /* 흰 배경 위 얇은 마크·선·아이콘 */
  --brand-soft:   #FFF3E4;  /* 틴트 면 (약) */
  --brand-soft-2: #FFE7CC;  /* 틴트 면 (강) */

  /* 잉크 */
  --ink:    #191919;
  --ink-2:  #353535;
  --ink-3:  #767676;   /* 본문 색 하한 */
  --dim:    #999999;   /* 큰 글자·장식 전용 */

  /* 면·선 */
  --paper:     #FEFDFB;  /* 페이지 플레인 (아주 옅은 웜) */
  --surface:   #FFFFFF;  /* 카드 */
  --panel:     #F5F5F5;
  --line:      #E4E4E4;
  --mute-fill: #D4D6D9;  /* 비강조 채움 (before 막대) */
}
```

### 2-2. 대비 실측표 (WCAG, 계산값)

| 색 | vs `#FFF` | vs `#191919` | 쓸 수 있는 곳 |
|---|---|---|---|
| `--brand` `#FF8F1C` | **2.28** | 7.70 | 큰 면, 알약 태그, 밴드. **흰 배경 위 작은 텍스트 금지**. 어두운 면 위 텍스트는 가능 |
| `--brand-ink` `#B85A05` | **4.66** | 3.77 | 흰 배경 위 오렌지 본문 텍스트 (AA 통과) |
| `--brand-mark` `#D2700A` | **3.46** | 5.08 | 흰 배경 위 얇은 선·아이콘·큰 텍스트 (≥3:1) |
| `--ink` `#191919` | 17.58 | — | 제목·본문 |
| `--ink-2` `#353535` | 12.27 | — | 본문 |
| `--ink-3` `#767676` | **4.54** | — | 보조 텍스트 **하한** |
| `--dim` `#999999` | **2.85** | — | 24px 이상 큰 글자, 장식선. **캡션 본문에 쓰지 말 것** |

> 기존 포트폴리오는 12–13px 캡션에 `#a8a8a8`(2.3:1) 을 여럿 썼습니다. 화면에선 넘어가도
> 인쇄하면 읽히지 않습니다. 이번에는 **캡션 최소 13px / `--ink-3`** 를 지킵니다.

> **`--dim` 은 결국 CSS 에서 전부 걷어냈습니다.** 이 문서는 처음부터 "24px 이상 큰 글자·장식
> 전용" 이라고 썼는데, 실제 사용처는 11–19px 인 단계 라벨·축 눈금·인덱스 번호뿐이었습니다.
> 규칙이 스스로를 금지하고 있었던 셈이라 전부 `--ink-3` 로 올렸습니다. 토큰은 남겨 두되
> **새로 쓰지 않습니다.**

### 2-2-1. 오렌지는 세 단계로 쓴다

토큰이 셋인데 전부 같은 강도로 쓰면 위계가 생기지 않습니다.
한때 아이브로우 대시 · 태그 · 스텝 점 · RESULT 수치 · 막대 · 버튼이 모두 `--brand` 였고,
그래서 열두 섹션에 "가장 중요한 숫자" 가 열두 개 생겼습니다. 절정이 없다는 뜻입니다.

| 단계 | 토큰 | 쓰는 곳 | 빈도 |
|---|---|---|---|
| **면** | `--brand` | 그 파트의 진짜 결론 (`.asym` 블록, `.rda` 채움, 히어로 버튼) | **파트당 1회** |
| **마크** | `--brand-mark` | 아이브로우 대시, 레일 선, 얇은 구분선 | 자유 |
| **잉크** | `--brand-ink` | 텍스트 강조, 태그 | 자유 |

얇은 선은 `--brand-mark` 가 대비도 더 좋습니다 — 흰 배경에서 **3.46:1 vs 2.28:1**.
위계를 정리하면서 접근성도 같이 올라갑니다.

### 2-3. 브랜드 아이덴티티 색 (데이터 색 아님)

| 브랜드 | 색 | 용도 |
|---|---|---|
| 지누스 | `#0E5B44` | 로고 옆 타임라인 막대, 섹션 라벨 |
| 샤크닌자 | `#141414` | 〃 |
| F&F 디스커버리 | `#1B3560` | 〃 |

**이 세 색을 차트 계열색으로 쓰지 않습니다.** dataviz 검증기에 4색(브랜드 오렌지 포함)을
categorical 팔레트로 넣어보면 명도대·채도 하한·정상시야 분리가 전부 FAIL 합니다
(`#0E5B44`↔`#1B3560` 정상시야 ΔE 14.7 — 15 미만). 아이덴티티와 데이터는 역할이 다릅니다.

---

## 3. 차트 규칙

이 사이트의 차트는 거의 전부 **"이 숫자가 올랐다"** 가 요지입니다.
그러면 정답은 categorical 이 아니라 **emphasis** — 하나만 브랜드색, 나머지는 회색입니다.

### 하면 안 되는 것

- 이중 축(y축 2개) — 단위가 다르면 차트를 나누거나 공통 기준으로 지수화
- 시리즈 색 돌려쓰기, 9번째 색 만들어내기
- 무지개 램프, 발산 팔레트 중간에 색상 넣기
- 모든 데이터 점에 숫자 달기
- 계열색을 텍스트에 쓰기 — 값·라벨·범례는 잉크 토큰, 색은 옆의 마크가 담당

### before → after

`--mute-fill` (before) + `--brand` (after). **값은 항상 막대 위 직접 라벨.**
브랜드 오렌지가 흰 배경에서 3:1 미만이라 relief rule 상 라벨이 선택이 아니라 **의무**입니다.

### 퍼널·단계 (오디널 램프)

단일 색상, 밝음 → 어두움. dataviz 검증기 **전 항목 PASS** 조합:

```
#F2A340  →  #E5850F  →  #BC6605  →  #8F4C03  →  #6B3802
```

(단일 색상 스프레드 10°, 인접 ΔL 전부 ≥0.06, 밝은 끝 대비 2.08:1 ≥ 2:1)

### 마크 사양

- 막대 데이터 끝 4px 라운드, 베이스라인에 붙임
- 선 2px, 마커 ≥8px
- 인접 채움 사이 2px 면색 간격
- 그리드·축은 뒤로 물러남 (`--line`, 1px)
- 2계열 이상이면 범례 필수, 4계열 이하는 직접 라벨도 병기

---

## 4. 레이아웃

### 4-1. 골격

```
콘텐츠 최대폭   1440px
좌우 여백       clamp(24px, 5vw, 96px)
컬럼            12 / 거터 28px   (768 아래 4컬럼)
섹션 상하 여백  clamp(72px, 9vw, 160px)
```

- **Split 은 좌우 바닥을 맞춥니다.** 컬럼을 행 높이만큼 늘려(`align-items:stretch`)
  세로 선이 영역을 먼저 긋게 하고, 남는 여백은 흩뿌리지 않고 **마지막 요소 위 한 곳**에
  몰아 바닥을 맞춥니다(`.split-r > :last-child{margin-top:auto}`).
  > 여백이 여러 곳에 조금씩 있으면 실수로 보이고, 한 곳에 모이면 의도로 보입니다.
  > 예전에는 결과 컬럼을 `sticky` 로 붙였는데, 그러면 컬럼이 내용 높이가 되어
  > **세로 선이 내용 끝에서 끊기고** 좌우 바닥이 어긋난 채 끝났습니다. 그게 "완성도가 낮다"의 정체였습니다.
- **섹션은 스크롤 단위**입니다. 고정 높이가 아니라 `min-height` 기준으로 내용만큼 늘어납니다.
- 페이지 표시는 좌측 세로 스파인이 아니라 **고정 상단바 + 상단 2px 진행 레일**입니다.
- 플레인은 순백이 아니라 `--paper` `#FEFDFB`. 그 위에 흰 카드가 떠 보입니다.

### 4-2. 브레이크포인트

| 폭 | 처리 |
|---|---|
| ≥1440 | 기준 레이아웃 |
| 1024–1439 | 컬럼 폭만 축소, 구조 유지 |
| 768–1023 | Split 2단 → 비율 완화, 소재 월 5열 → 3열 |
| <768 | 전부 1단, 소재 월 2열, 상단바 앵커는 접음 |

### 4-3. 간격 스케일 (8px 기반)

`4 · 8 · 12 · 16 · 24 · 32 · 40 · 56 · 72 · 96 · 128 · 160`

여기 없는 값은 쓰지 않습니다.

### 4-4. 라운드

| 토큰 | 값 | 용도 |
|---|---|---|
| `--r-sm` | `8px` | 태그, 작은 칩 |
| `--r` | `20px` | 카드 기본 |
| `--r-asym` | `20px 0` | **시그니처.** 섹션당 최대 1–2개, 강조 카드에만 |
| `--r-pill` | `999px` | 알약 라벨, 버튼 |
| `--r-full` | `50%` | 프로필 원형 |

> 비대칭 코너는 아껴 써야 효과가 납니다. 모든 카드에 걸면 그냥 노이즈가 됩니다.

---

## 5. 타이포

`Pretendard Variable` (브랜드 사이트와 동일) → `Pretendard` → `Apple SD Gothic Neo` → `sans-serif`

| 역할 | 크기 | 굵기 | 행간 | 자간 |
|---|---|---|---|---|
| display | `clamp(56px, 9vw, 168px)` | 800 | 0.95 | -0.05em |
| h1 | `clamp(34px, 4.4vw, 68px)` | 800 | 1.12 | -0.04em |
| h2 | `clamp(26px, 2.6vw, 42px)` | 700 | 1.2 | -0.035em |
| h3 | `clamp(20px, 1.8vw, 28px)` | 700 | 1.3 | -0.03em |
| lead | `clamp(17px, 1.4vw, 22px)` | 400 | 1.65 | -0.015em |
| body | `17px` | 400 | 1.6 | -0.01em |
| small | `15px` | 400 | 1.6 | 0 |
| caption | `13px` | 400 | 1.55 | 0 |
| label | `11px` | 700 | 1.3 | **0.22em**, 대문자 |

**공통 규칙**

- `word-break: keep-all` — 한글 단어가 중간에서 끊기지 않게
- 숫자가 세로로 정렬돼야 하는 표·축에만 `font-variant-numeric: tabular-nums`
- 본문 한 줄 최대 **72자**. 넘으면 컬럼을 좁힙니다
- **본문 글자 수를 기존 포트폴리오 대비 40% 줄입니다.** 밀도를 낮추는 게 리디자인의 절반입니다

---

## 6. 섹션 아키타입

같은 구성이 **세 번 연속 나오지 않게** 배치합니다. 리듬이 없으면 스크롤이 지루해집니다.

| 아키타입 | 구성 | 쓰는 곳 |
|---|---|---|
| **A. Statement** | 대형 타이포 한 덩어리, 여백이 8할 | 히어로 · 파트 전환 · 마감 |
| **B. Split** | 좌 판단/과정 · 우 결과 (6:4 ~ 7:3 가변) | 프로젝트 상세 |
| **C2. Archive** | 브랜드 라벨 + 대형 그리드 | 소재 아카이브 |
| **C. Showcase** | 소재·영상이 주인공, 텍스트는 얇은 캡션으로 물러남 | Creative |
| **D. Grid** | 균등 카드 분할 | 프로필 · 운영 개요 · 인덱스 |
| **E. Full-bleed** | 컨테이너를 벗어나 화면 폭을 다 씀 (`.bleed`) | **소재 아카이브** — 양이 곧 주장인 블록 |

> **프로세스 레일은 페이지 전체에서 여덟 번 나옵니다.** 이걸 축을 돌려(세로 변형) 해결하려다
> 실패했습니다 — §7 을 보세요. 반복 자체를 줄이는 게 아니라 **반복돼도 버티는 컴포넌트**를
> 만드는 쪽이 맞습니다. 리듬은 Full-bleed 같은 아키타입 교체로 냅니다.

---

## 7. 컴포넌트

### 상단바 `.topbar`
고정. 이름 · 섹션 앵커 · PDF 버튼. 스크롤 시 높이 72 → 56px, 배경 반투명 + `backdrop-filter`.
768 아래에서 앵커는 숨기고 이름과 PDF만 남깁니다. **인쇄 시 숨김.**

### 진행 레일 `.rail`
상단 2px, `--brand`. 스크롤 비율. **인쇄 시 숨김.**

### 영상 카드 `.vcard` (신규)
```
.vcard          래퍼. aspect-ratio 로 규격 고정 (9/16, 1/1, 16/9)
.vcard video    poster 필수. muted loop playsinline preload="metadata"
.vcard .spec    우상단 규격 배지 (알약, --brand-soft-2 / --brand-ink)
.vcard .cap     하단 캡션 (13px, --ink-3)
```
- **poster 는 필수입니다.** 인쇄와 JS 미실행 시 이 이미지가 그대로 나갑니다
- `muted` + `playsinline` + `loop` — 자동재생 정책에 걸리지 않는 조합입니다
- 자동재생은 뷰포트 진입 시에만(`IntersectionObserver`, threshold 0.25). 이탈하면 `pause()`
- **탭을 떠났다 돌아오면** 브라우저가 재생을 멈춰 둡니다. 교차 이벤트는 새로 안 생기므로
  `visibilitychange` 에서 화면 안 영상을 직접 되살립니다
- 컨트롤은 숨기되 클릭하면 재생/정지 토글
- 규격 배지는 **영상 위가 아니라 캡션에** (`.vspec`) — 소재를 가리지 않기 위해서입니다

### 스탯 타일 `.stat`
큰 수치 + 단위 + 한 줄 설명. 수치는 `clamp(40px, 4vw, 64px)` / 800.
카운트업 대상이므로 `tabular-nums` 로 폭을 고정해 레이아웃이 흔들리지 않게 합니다.

### 단계 `.flow` · `.loop`
**둘은 같은 물건입니다.** 위 규칙 + 번호 + 제목 + 설명. 그래서 한 규칙으로 묶었습니다.

4단계. **네모 상자를 쓰지 않습니다.** 단계마다 자기 눈금(2px `--ink`)을 갖고 그 아래로
번호 · 제목 · 설명이 매달립니다. 네 눈금이 나란히 서면 그 자체가 '네 구간을 지난다'가 됩니다.
강조 단계(`.on`)는 눈금을 `--brand`, 번호·제목을 `--brand-ink` 로 올립니다.

> **점을 꿴 실은 걷어냈습니다.** 원래는 하나로 이어진 선 위에 점을 찍었는데,
> 점 하나만 주황이면 "저건 왜 강조지?" 가 먼저 옵니다 — 실제로 그 질문을 받았습니다.
> 점은 아무것도 뜻하지 않았고, 레일 한 구간만 칠하면 "3번에서 4번으로 가는 길"로 읽혔습니다.
> **강조는 구간에 걸고, 장식은 없앱니다.**

> 처음에는 검은 채움 박스를 썼는데, 모바일에서 카드가 세로로 쌓이니 답답하고 싸 보였습니다.
> 상자를 걷어내니 단계가 '흐름'으로 읽힙니다. **강조는 면이 아니라 색으로만** 합니다.

**세 가지가 이 컴포넌트를 지탱합니다.**

1. **레일이 보여야 합니다.** 2px `--line`(#E4E4E4)은 회색 패널(#F5F5F5) 위에서 대비 1.09 —
   있으나 마나였습니다. **3px `--mute-fill`** 로 두 배경 모두에서 선이 선으로 읽힙니다.
2. **강조는 점 하나가 다 맡습니다.** 레일 한 구간만 주황으로 칠하면 "3번에서 4번으로 가는 길"
   처럼 읽혀 뜻이 흐려집니다. 레일은 끝까지 한 톤, 표시는 점을 키우고(19px) 후광을 답니다.
   후광은 `--brand-soft-2` — `--brand-soft` 는 `--paper` 위에서 보이지 않습니다.
3. **제목이 실체입니다.** 19px 은 본문(17px)보다 겨우 크고 설명(14px)과도 붙어 있어
   네 단계가 '회색 문단 네 덩어리'로 읽혔습니다. 11 / 25 / 14 로 벌립니다.

> **컨테이너가 기준입니다, 뷰포트가 아니라.** `.split` 안에 들어간 레일은 1440 기준으로
> 한 칸이 **160px** 밖에 안 됩니다(좌측 767 ÷ 4 − 여백). 여기서 25px 을 쓰면
> "OAuth 매체 연동" 같은 제목이 어색하게 끊깁니다. 그래서 `.split .flow` 는 21px 입니다.
> **이 컴포넌트를 더 키우고 싶으면 폰트가 아니라 담는 폭을 먼저 바꿔야 합니다.**

> **한때 세로 변형(`.flow--v`)과 세로 퍼널(`.fnl`)을 만들었다가 걷어냈습니다.**
> 여덟 번 반복되는 게 지루하니 축을 돌려 리듬을 주자는 발상이었는데,
> 앞뒤가 전부 가로인 페이지에서 한 곳만 세로로 꺾이니 "왜 갑자기 세로냐"가 먼저 보였습니다.
> 퍼널도 폭이 좁아지지 않으니 퍼널로 읽히지 않고, 가로 200px 이면 될 것이 1,000px 을 썼습니다.
> **축을 바꿔 변화를 만들지 말고, 반복돼도 버티는 물건을 만드는 게 맞습니다.**

### 비대칭 비교 `.asym`
"39% 줄였는데 10%만 빠졌다" 는 **격차**가 요지입니다. 같은 크기 상자 두 개를 `≫` 글자로 잇는
방식은 그 격차를 그리지 않고 말로만 합니다. **같은 축 위에 실제 비율(39 : 10)로 긋습니다.**
막대 길이가 눈금이 아니라 값 그 자체입니다.

### 재배분 도식 `.rda`
공유예산처럼 **한 덩어리를 나눠 쓰는 구조**를 보여줄 때. 하나의 트랙을 경계선(`.seam`)으로
가르고, 그 경계가 오른쪽으로 밀린다는 걸 손잡이로 표시합니다.
아래 점선(`.rda-return`)이 "다시 측정해 기준을 갱신"하는 루프입니다.

- **경계 위치는 5:5 고정** — 비율을 다르게 그리면 없는 데이터를 주장하게 됩니다
- 트랙 높이는 92px. 124px 짜리 큰 면에 12px 글자가 뜨면 '대충 칠한 상자'로 보입니다
- 트랙에 1px 테두리를 둡니다 — 왼쪽 반이 `--panel` 이라 회색 면 위에 놓이면 배경에 녹습니다
- 손잡이 삼각형은 `clip-path` 로 그립니다. 투명 테두리로 삼각형을 만드는 옛 수법은
  '두꺼운 한쪽 테두리'로 남아 다른 규칙과 헷갈립니다

### 수치 눈금 `.card--tick`
값 네 칸을 상자에 담을 이유가 없습니다. 테두리 · 라운드 · 면을 걷어내면 바로 위 단계
컴포넌트와 같은 어휘(눈금 + 값)가 되어 한 덩어리로 읽힙니다.
**한 섹션에서 상자가 또 네 개 나오면 '카드가 페이지 구조'가 되어 버립니다.**

### 소재 월 `.wall`
정사각 타일 그리드. 5열 → 3열 → 2열. `gap: 12px`, 타일 `--r-sm`.
크롭이 생기지 않게 원본 비율(1:1)과 타일 비율을 맞춥니다.
**16:9 소재를 1:1 타일에 넣지 마세요** — 카피가 잘립니다.

### 소재 아카이브 `.arch`
브랜드별로 묶어 보여주는 큰 그리드. 좌측 라벨 컬럼(200px, `sticky`) + 5열 타일.
브랜드당 10종으로 맞춰 2행이 정확히 채워지게 합니다 — 마지막 줄이 비면 미완성으로 보입니다.
인쇄에서는 라벨을 위로 빼고 그리드가 지면을 다 쓰게 해 **브랜드 한 곳이 한 장**을 차지합니다.

### 영상 행 `.vrow`
규격이 다른 영상을 **같은 높이로** 세우는 행. `flex` + `wrap` 은 캡션 길이에 따라 폭이 흔들려
마지막 칸이 다음 줄로 떨어졌습니다. **컬럼 비율을 비율값으로 못박으면** 높이가 저절로 맞고 줄바꿈도 없습니다.

| 변형 | 컬럼 | 쓰는 곳 |
|---|---|---|
| `.vrow--mix` | `0.5625fr 1fr 1fr` | 9:16 하나 + 1:1 둘 (9÷16 = 0.5625) |
| `.vrow--v3` / `--v4` | `repeat(3\|4, 1fr)` | 세로 영상만 나열 |

### 결론 섹션 — 다크 전환 · 결론 제목
회사 분석은 이 문서의 결론입니다. **면을 어둡게 끊어 집중을 올립니다.**

- **이음매는 660px 롱 그라데이션.** 224px 로 잡았더니 조명이 꺼지는 게 아니라
  선이 하나 그어진 꼴이었습니다. 앞 섹션은 밝고 뒤(Contact)는 이미 어두우니 **위쪽만** 이어 줍니다
- **정지점을 여러 개** 둡니다(140·300·450·560·660px). 밝→어두 2점 보간은
  가운데가 탁한 회색으로 주저앉습니다
- **결(grain)을 얹습니다.** 긴 어두운 그라데이션은 8비트 계조에서 띠가 보입니다 —
  SVG `feTurbulence` 를 data URI 로 넣어 요청 없이 흩습니다
- **`%` 와 `px` 를 섞지 않습니다.** `%` 는 섹션 높이 기준이라 이 섹션(2000px+)에서는
  중간 정지점이 최종점보다 뒤로 갑니다
- 다크 위에서는 `--brand-ink`(3.77:1) 대신 **`--brand`(7.70:1)** 를 씁니다
- **글자 색은 면 이름이 아니라 그 자리의 실제 밝기로 정합니다.** 다크 섹션이라고
  소재 캡션에 흰 글자를 썼더니, 그 자리는 아직 밝아서 사라졌습니다

**결론 제목** — h2 기본(42px 상한)으로는 소재가 쓸려 나간 자리를 못 받습니다.
이 섹션에서만 `clamp(29px,4.3vw,62px)` 로 올리고 동사(`뜯어봤습니다`)에 브랜드색을 줍니다.

> **글자를 자르는 연출은 쓰지 않습니다.** `clip-path` 로 걷어 올렸더니 스크롤 도중
> 글자 한가운데가 가로로 잘렸습니다. '걷히는 중' 이 아니라 **'깨진 렌더'** 로 보입니다.
> 올라오면서 또렷해지는 것으로 충분합니다.

**가운데 정렬은 짧은 것만.** 제목·리드·수치는 모으되 관찰 카드 본문과 90일 계획 목록은
왼쪽에 둡니다 — 여러 줄 문단을 가운데 정렬하면 줄 시작점이 매 줄 달라져 읽는 속도가 떨어집니다.
**가운데 정렬은 레이아웃의 문제이지 문단의 문제가 아닙니다.**

### 스크롤 도입 `.adintro`
회사 분석 섹션 맨 앞에 광고 라이브러리의 숏폼 소재를 **5열 2행**으로 깝니다.
스크롤을 내리면 소재가 흐려지며 물러나고 분석이 들어옵니다.
**근거가 아니라 "쭉 넘겨봤다" 는 인상**입니다. 그래서 캡션도 설명도 달지 않습니다.

- **기본 상태가 곧 최종 상태입니다.** 스크롤 애니메이션을 지원하지 않는 브라우저,
  모션을 끈 사람, 인쇄 — 어디서든 그냥 소재 격자로 남아 읽는 데 지장이 없습니다
- `animation-timeline: view()` + `animation-range: exit` — JS 없이 스크롤에 반응합니다.
  `@supports` 로 감싸 지원 여부가 곧 분기입니다
- **들어옴 → 머무름 → 쓸려 나감을 하나의 궤적**으로 묶습니다. 나타났다 사라지기만 하면
  '슬라이드 넘김' 이지 '넘겨보는 것' 이 아닙니다
- 열마다 빠지는 방향을 **부채꼴**로 벌립니다 — 가운데는 위로, 바깥은 옆으로 기울며.
  카드를 손으로 쓸어 치우는 결이 나옵니다
- **체류를 길게, 쓸림을 짧게.** 키프레임을 `7% 진입 · 64% 까지 정지 · 93% 에 소멸` 로 두면
  같은 스크롤 거리에서 훨씬 빠르게 느껴집니다. 구간을 넓히면 오히려 굼떠 보입니다
- **`blur` 는 쓰지 않습니다.** 열 장을 매 프레임 다시 래스터하면 끊깁니다 —
  `transform` 과 `opacity` 만 쓰면 합성 단계에서 끝납니다
- **재생 버튼이 없어야 합니다.** 화면을 잘라 쓰면 가운데 버튼이 남습니다 —
  `<video>` 의 `poster` 원본 주소를 받아 씁니다
- 10장을 5열로 두어 **2행이 정확히** 채워집니다 (§7 소재 아카이브와 같은 이유)

> **레일·띠로 세 번 실패한 뒤에 온 답입니다.** 카드 뒤에 깔면 카드가 덮어 안 보이고,
> 좌우에 절대배치로 얹으면 화면 폭에 따라 글자를 덮습니다.
> 칸을 나눠 옆에 세우면 겹치지는 않지만 섹션만 좁아져 어색합니다.
> **배경으로 우겨넣을 자리를 찾을 게 아니라, 아예 앞에 세웠다가 비켜 주면 됩니다.**

### 이월 도식 `.carry`
관리표가 다음 달로 넘어가는 구조. **글로 또 나열하지 않습니다** — 바로 위 4단과 같은 말이 됩니다.
표 두 장을 놓고 채워진 칸이 TO-BE 에서 다음 달 AS-WAS 로 자리를 옮기는 것으로 보여줍니다.

### 소재 카드 `.card--img`
설명만 있는 상자 대신 **근거가 되는 소재를 머리에 얹은 카드**. 이미지 1:1 → 본문 → 인용(`.q`).
카피 축처럼 "말한 것"과 "실제 소재"가 붙어야 하는 곳에 씁니다.

### 태그 `.tag`
알약. `--brand-soft` 배경 + `--brand-ink` 텍스트. 11px label 스케일.

### 그 외
`섹션 헤더` · `파트 전환` · `프로필 카드` · `인덱스` · `결과 컬럼` · `퍼널` · `프로세스 4단` ·
`스택 레이어` · `비교 막대` · `before/after 그룹 막대` · `화면 프레임` · `인용/노트` · `푸터`

---

## 8. 모션

**원칙 — 스크롤을 방해하지 않는다.** 패럴랙스 남용·스크롤재킹·회전·바운스는 넣지 않습니다.
전부 `IntersectionObserver` 하나로 처리하고 **한 번만 실행**합니다(재진입 반복 없음).

| 모션 | 대상 | 사양 |
|---|---|---|
| 진입 페이드업 | 섹션 헤더·카드 | `opacity 0→1`, `translateY 20px→0`, 480ms, 자식 60ms 스태거 |
| 막대 성장 | 차트 막대·타임라인 | `width/height 0→값`, 700ms `cubic-bezier(.22,1,.36,1)` |
| 숫자 카운트업 | 스탯 타일 | 900ms, `tabular-nums` |
| 영상 자동재생 | 영상 카드 | 진입 시 `play()`, 이탈 시 `pause()` |
| 진행 레일 | 상단 2px | 스크롤 비율 |

`prefers-reduced-motion: reduce` 면 **전부 끕니다.**

### PDF 안전장치 3중

1. 모션 CSS 는 전부 `@media screen` 안에만 둡니다
2. `@media print { *{ animation:none!important; transition:none!important;
   opacity:1!important; transform:none!important } }`
3. **초기 상태를 JS 가 `.motion` 클래스로 덧씌우는 방식(additive).**
   JS 가 안 돌면 모든 요소가 처음부터 최종 상태입니다

> 3번이 핵심입니다. "숨겼다가 JS 로 보여주는" 방향이면 JS 실패 시 빈 페이지가 나옵니다.
> 반대 방향으로 만듭니다.

---

## 9. 인쇄 (PDF 파생)

```css
@page { size: 1440px 1020px; margin: 0 }
```

- 상단바·진행 레일·앵커·스크롤 힌트 숨김
- 섹션마다 `break-after: page`, 내부 `break-inside: avoid`
- 2단 레이아웃은 인쇄에서도 유지 (모바일 1단으로 접히지 않게 `@media print` 에서 폭 고정)
- 영상은 poster 프레임으로 렌더 — **첫 출력 때 반드시 육안 확인**
- 출력: Chrome headless `--print-to-pdf`, `Start-Process -Wait`, 경로 공백은 `%20`

---

## 10. 카피 원칙

**디자인만큼 중요한 축입니다.** 제목·리드문·캡션·이력서 문장 전부 여기 따릅니다.

### 하지 않을 것

- 과장 형용사 — "혁신적", "압도적", "극대화", "탁월한", "성공적으로"
- 자기 홍보 문구 — "~를 통해 성과를 창출했습니다", "적극적으로 리드했습니다"
- AI 문체 — 모든 불릿이 같은 길이·같은 동사꼴, 과한 병렬 구조, "~함으로써"
- 근거 없는 수식 — 숫자가 없으면 형용사로 메우지 않고 그냥 뺍니다

### 할 것

- **결론 먼저, 근거 뒤** — "월 6억을 굴렸습니다" 다음에 "패션, 약 1년"
- **문장 길이를 일부러 흔든다** — 긴 문장 옆에 짧은 문장 하나를 툭
- **담백한 동사** — 만들었습니다 / 바꿨습니다 / 남겼습니다 / 없앴습니다
- **고유명사와 숫자가 형용사를 대체한다** — "많은 소재"가 아니라 "1:1·9:16·16:9 세 규격"
- **기여도를 정직하게** — "기획 참여 · 디자인팀 협업 제작", "기여도 50%"
- **화면에 보이는 것과 문장이 어긋나지 않게** — "혜택을 먼저"라고 썼으면 옆 소재에 할인율이 보여야 합니다
- **같은 수치를 두 곳에서 반복하지 않기** — 근거(차트)가 있는 쪽에만 두고 다른 곳은 링크로 넘깁니다
- **가진 것보다 크게 말하지 않기** — 열 점만 골랐으면 "2년간 만든 소재"가 아니라 "제작 대표 소재"

### 톤 기준선

> "스스로 질문하고, 개선점을 찾고, 효율적으로 실행합니다."
> "이긴 것은 상시 운영으로 올리고 진 것은 제외 액션으로 확정해, 판단을 계정에 남겼습니다."

반대로 ~~"데이터 기반의 전략적 사고를 통해 광고 효율을 극대화하였습니다"~~ 는 쓰지 않습니다.

---

## 11. 콘텐츠 원칙 (기존 저장소에서 계승)

- 본인 재직·운영 기간 밖 데이터는 쓰지 않습니다
- 원자료에 없는 수치는 만들지 않습니다
- **대외비 금지** — 매체 마크업률, 솔루션 단가, 견적 금액, 광고주 실지출·실매출
- 기여 범위를 부풀리지 않습니다 — 광고 영상은 "기획 참여 · 디자인팀 협업 제작"
- 이 저장소는 public 입니다. `robots.txt` 는 noindex 지만 URL 을 아는 사람은 볼 수 있습니다
